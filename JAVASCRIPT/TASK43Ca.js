const bag = "BLUE";

function changeCase(text) {
    const toLowerCase = text.toLowerCase();
    console.log(toLowerCase);
}
changeCase(bag);

const gains = "weightgain is overvalued in nigeria ";

function toRepeat(writeup) {
    const toRepeat = writeup.repeat(5);
    console.log(toRepeat);
}
toRepeat(gains);

